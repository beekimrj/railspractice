<h1> Add New paragraph </h1>
<% flash.each do |key, value| %>
       <font color = "red"> <div class="<%= key %>"><%= value %></div> </font>
<% end %>
<%= render partial: "paragraphForm", locals: {paragraphURL: paragraphs_path, formMethod: :post} %>